package Collection;

import java.util.ArrayDeque;
import java.util.PriorityQueue;

public class Queue {
    public static void main(String[] args) {

        // ArrayDeque

        ArrayDeque<Integer> ad = new ArrayDeque<>();
        // pop,poll,pollFirst,pollLast,removeFirst,removeLast - removes element and
        // returns it.Emptylist - (other - throws exception,poll - null)

        // push,add,addFirst,addLast,offer,offerFirst,offerLast - adds element and some
        // returns boolean.
        // Deque parent interface ->

        // PriorityQueue

        // PriorityQueue represented as balanced Binary Heap
        PriorityQueue<Integer> pq = new PriorityQueue<>();

        // pop,poll,pollFirst,pollLast,removeFirst,removeLast - removes element and
        // returns it.Emptylist - (other - throws exception,poll - null)

        // push,add,addFirst,addLast,offer,offerFirst,offerLast - adds element and some
        // returns boolean.
        // Queue parent interface
    }
}
